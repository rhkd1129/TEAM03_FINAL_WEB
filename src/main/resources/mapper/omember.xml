<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
	
	<mapper namespace="omember">
		<insert id="insert" parameterType="omember">
			insert into owner (oid, opassword, oname, otitle, oadress, otel, ocategory)
			values (#{oid}, #{opassword}, #{oname}, #{otitle}, #{oadress}, #{otel}, #{ocategory})
		</insert>
	</mapper>